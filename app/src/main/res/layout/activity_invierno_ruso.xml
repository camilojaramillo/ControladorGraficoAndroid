<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.camilo.tallerdos_per_jaramillo.InviernoRuso"
    android:background="@drawable/pantalla">

    <ImageButton
        android:layout_width="110dp"
        android:layout_height="70dp"
        android:text="Arriba"
        android:id="@+id/arr"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="97dp"
        android:src="@drawable/arr"
        android:background="#00ffffff" />

    <ImageButton
        android:layout_width="110dp"
        android:layout_height="70dp"
        android:text="Derecha"
        android:id="@+id/der"
        android:layout_centerVertical="true"
        android:layout_toEndOf="@+id/arr"
        android:src="@drawable/der"
        android:background="#00ffffff"/>

    <ImageButton
        android:layout_width="110dp"
        android:layout_height="70dp"
        android:text="Izquierda"
        android:id="@+id/izq"
        android:layout_centerVertical="true"
        android:layout_toStartOf="@+id/arr"
        android:src="@drawable/izq"
        android:background="#00ffffff"/>

    <ImageButton
        android:layout_width="110dp"
        android:layout_height="70dp"
        android:text="Abajo"
        android:id="@+id/aba"
        android:layout_below="@+id/der"
        android:layout_alignStart="@+id/arr"
        android:layout_marginTop="40dp"
        android:src="@drawable/aba"
        android:background="#00ffffff"/>
</RelativeLayout>
